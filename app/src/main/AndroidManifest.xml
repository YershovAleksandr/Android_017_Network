<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nam.mynetwork">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NetworkingURLActivity" />
        <activity android:name=".NetworkingSocketsActivity" />
        <activity android:name=".NetworkingAndroidHttpClientActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".NetworkingAndroidHttpClientJSONActivity" />
        <activity android:name=".NetworkingAndroidHttpClientXMLActivity" />
        <activity android:name=".MA" />
    </application>

</manifest>